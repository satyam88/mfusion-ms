apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dev-mfusion-ms-ingress
  namespace: dev
  labels:
    env: dev
    app: mfusion-ms-ms
    layer: frontend
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: dev-mfusion-ms.connectingclouds.in
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: dev-mfusion-ms-service
                port:
                  number: 8080
