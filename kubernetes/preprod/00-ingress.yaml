apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: preprod-mfusion-ms-ingress
  namespace: preprod
  labels:
    env: preprod
    app: mfusion-ms-ms
    layer: frontend
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: preprod-mfusion-ms.connectingclouds.in
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: preprod-mfusion-ms-service
                port:
                  number: 8080
